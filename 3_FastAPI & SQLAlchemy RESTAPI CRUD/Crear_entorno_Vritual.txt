python -m venv venv

2- Activar entorno virtual
    - acceder la ruta/ven/scripts/activate
    - ./venv/Scripts/activate

3- prueba de instalacion de paquetes al entorno virtual
    - python.exe - pip install --upgrade pip

4- Extraer requerimientos
    - pip freeze > requirements.txt

5- Desactivar entorno virtua
    - desactivate (en consola)

6- INstalar requirements.txt en otro entorno virtual
    - pip install -r (ruta de requirements.txt -> ejemplo ) .\requirements.txt